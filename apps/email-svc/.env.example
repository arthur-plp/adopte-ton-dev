DATABASE_URL=postgresql://atd:atd_dev_password@localhost:5432/atd_email
CLOUDAMQP_URL=amqp://atd:atd_dev_password@localhost:5672
PORT=3008

# Base des liens cliquables dans les emails (front Next.js)
WEB_URL=http://localhost:3000

# Resolution des destinataires (nom/email) et du contexte (titre d'offre)
AUTH_SVC_HOST=localhost
AUTH_SVC_PORT=3001
JOBS_SVC_HOST=localhost
JOBS_SVC_PORT=3002

# Resend
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
RESEND_FROM_EMAIL=Adopte Ton Dev <onboarding@resend.dev>
# En dev, les emails sont simulés (loggés, pas envoyés) sauf si forcé :
RESEND_FORCE_SEND=false
